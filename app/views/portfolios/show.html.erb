<h1>Portfolio Show</h1>

<p><%= @portfolio_items.title %></p>

<p><%= @portfolio_items.subtitle %></p>

<p><%= @portfolio_items.body %></p>

<p><%= image_tag @portfolio_items.main_image unless @portfolio_items.main_image.nil? %></p>

<%= link_to 'Back', portfolios_path %>

<%= link_to 'Edit', edit_portfolio_path(@portfolio_items) %>

<%= link_to 'Delete', portfolio_path(@portfolio_items), method: :delete %>
